import { GetBookingDetails } from './get-booking-details';

describe('GetBookingDetails', () => {
  it('should create an instance', () => {
    expect(new GetBookingDetails()).toBeTruthy();
  });
});
